/**
 * polymorphism->representing one entity in multiple forms.
 * 
 * same name but different forms
 * 2 types:-
 * compile time polymorphism(prebound and predefined)-2 types->method overloading/ function overloadingand operator overloading
 * run time polymorphism(postbound)->method overriding (in that we have virtual function)
 * 
 */